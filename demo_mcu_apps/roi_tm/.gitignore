# trainer artifacts: tm_model.h is the committed default build input;
# tm_model.json is redundant inspection output, regenerated by train_tm.py
tm_model.json
_test_model.h
samples_baseline.jsonl
samples_hard.jsonl
